version: 0.2

phases:
  install:
    runtime-version:
      nodejs: 10
  pre_build:
    commands:
      - echo Installing dependencies...
      - npm install
  build:
    commands:
      - echo Testing...
      - npm run test
      - echo Building...
      - npm run build

artifacts:
  files:
    - "**/*"
  discard-paths: yes
  base-directory: dist
